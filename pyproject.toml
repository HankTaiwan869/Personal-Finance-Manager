[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "financial-hub"
version = "1.3.0"
description = "Local desktop portfolio tracker for Taiwan securities"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "PyQt6>=6.8",
    "SQLAlchemy>=2.0.45",
    "httpx>=0.28",
    "keyring>=25.6",
    "plotly>=6.3",
    "PyQt6-WebEngine>=6.8",
    "pyxirr>=0.10.8",
    "openpyxl>=3.1",
    "platformdirs>=4.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-qt>=4.4",
    "ruff>=0.16.3",
]

[project.scripts]
financial-hub = "financial_hub.app:main"

[tool.hatch.build.targets.wheel]
packages = ["financial_hub"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
